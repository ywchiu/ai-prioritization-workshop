# 步驟三執行紀錄

執行日：2026-09-10。產出：[03_review_22_to_10.csv](03_review_22_to_10.csv)；本例為課程演練的 AI 整理表，非機關核定清單。

## 本次實際操作

1. 讀取 Step 1 的 22 個固定編號與中文名，並讀取 Step 2 的 660 列背景表；主表依空白範本輸出 12 欄、22 列，每個候選恰好一列。
2. Step 2 是縮時核讀：42 列含未帶缺口標記的具體背景，618 列帶有「待查／本次未蒐集／資料缺口」標記（其中包含部分參考值）。因此本表只引用可回查的 `02：疾病編號／層面／欄位`，未聲稱已完成全面證據蒐集。
3. 本次沒有真人學員或機關的人工選樣輸入。主表的處置均為「待人工決定」、提案組別均為「待人工填寫」，正式審查結果均留空；這不是 10 項清單。
4. 為測試下一步檔案承接，另建 [03_step4_flow_test_selection.csv](03_step4_flow_test_selection.csv)，固定 ID 為 1、2、3、4、5、6、11、14、16、22。每列標明「純流程測試選樣、無優先意義、未經人工核定」，不代表優先性，也沒有寫回主表。

## 已知限制與交接

- 來源 URL 與 2026-09-10 的本次查證日保留在主表；來源發布日未取得者仍標示待查。尚未把國內通報、WOAH 列管或雙部門監測推論成零或已確認。
- 登革熱 B1 與貓抓病 B2 不在 Step 1 的固定 22 列，未混入主表。登革熱不使用「沒有動物宿主」的錯誤絕對敘述；森林靈長類循環是範圍討論的限制。
- Step 4 如需正式課堂選樣，必須先由人填滿主表的處置、理由與提案組別，並使保留數恰為 10。它可以先用 fixture 測試流程，但不得將 fixture 說成學員或機關決定。

## 驗收

以 CSV 讀回檢查：主表 12 欄、22 列、編號 1–22 各一次、無空的理由／依據／缺口，正式審查欄全為空白，所有狀態都含「尚未送防檢署確認」。fixture 為 10 列且 ID 固定如上。

`materials/tests/test_lab_worked_example.py` 的完整交接測試由專案根目錄的 test discovery 執行；Step 3 交接測試通過後，才表示表格結構和狀態標記合格，不表示人工選樣或正式確認完成。

主 agent 覆核後修正了初版把部分未蒐集疾病寫成「02 已核讀」的說法；主表現由實際 01／02 列重建理由、精確欄位引用與缺口清單，沒有新增去留決定。
